**ROM** or **read-only memory** is a set of files and folders in the [[Virtual Filesystem|VFS]] which are written when the computer boots and cannot be edited or deleted afterwards.

Every computer has ROM accessible at `/rom`.